version: 1
frontend:
  phases:
    preBuild:
      commands:
        - yum install -y jq
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: public
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
